#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../angular-http-server/angular-http-server.js" "$@"
  ret=$?
else 
  node  "$basedir/../angular-http-server/angular-http-server.js" "$@"
  ret=$?
fi
exit $ret
